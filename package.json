{
  "name": "first-faith",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --names BACKEND,FRONTEND --prefix \"[{name}]\" --prefix-colors blue,green \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:backend": "npm run prisma:generate --prefix backend && npm run build --prefix backend",
    "build:frontend": "NODE_ENV=production npm run build --prefix frontend",
    "build": "npm run prisma:generate --prefix backend && npm run build --prefix backend && NODE_ENV=production npm run build --prefix frontend",
    "start": "npm run start:prod --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
